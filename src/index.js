// aqui van mis funciones
export const sum = (text, word) => {
    const concat = text + word;
    return concat;
};

export const restar = (text, word) => {
    const concat = text + word;
    return concat;
};